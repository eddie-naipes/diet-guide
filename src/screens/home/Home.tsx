import {Container} from "@screens/home/styles";
import {Header} from "@components/header/Header";

export const Home = () => {
    return (
        <Container>
            <Header/>
        </Container>
    );
};